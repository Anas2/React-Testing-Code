import React from 'react';
import { Box } from '@mui/material';

function Button(props) {
    // const { label } = props;

    return (
        <Box>
            <Button ></Button>
        </Box>
    );
}

export default Button;